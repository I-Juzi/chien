<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.juzi.chien.admin.mapper.SysMenuMapper">

    <!-- 根据用户ID查询菜单列表（通过用户->角色->菜单关联） -->
    <select id="selectMenusByUserId" resultType="com.juzi.chien.admin.domain.entity.SysMenu">
        SELECT DISTINCT m.*
        FROM `chien-menu` m
        INNER JOIN `chien-role_menu` rm ON m.id = rm.menu_id
        INNER JOIN `chien-user_role` ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
        ORDER BY m.sort_order
    </select>

    <!-- 根据角色ID查询菜单列表 -->
    <select id="selectMenusByRoleId" resultType="com.juzi.chien.admin.domain.entity.SysMenu">
        SELECT m.*
        FROM `chien-menu` m
        INNER JOIN `chien-role_menu` rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId}
        ORDER BY m.sort_order
    </select>

</mapper>
